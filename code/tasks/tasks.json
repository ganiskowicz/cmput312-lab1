{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "find ${workspaceFolder} -name '*.py' -exec chmod +x {} \\;",
            "problemMatcher": []
        }
    ]
}